# api

Single Flask service exposing all user-facing, frontend-facing, and
agent-internal REST endpoints. See `api&schema/api-structure-en.md`
for the full endpoint reference and `api&schema/*.sql` for the
database schema.
