[project]
name = "search-position-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Daniel", email = "Dan5204@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
search-position-agent = "search_position_agent:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
